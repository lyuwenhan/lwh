#include <iostream>
using namespace std;
int main(){
	double m, h;
	cin >> m >> h;
	double a = m / h 2 (40 <= m <= 120,1.4 <= h <= 2.0 );
	cout << "Today, I ate " << n << " apple";
	if(n > 1){
		cout << "s"; 
	}
	cout << "." << endl;
	return 0;
}
